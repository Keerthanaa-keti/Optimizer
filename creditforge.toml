# CreditForge Configuration
# Claude Subscription Optimizer & Autonomous Task Executor

[night_mode]
enabled = true
start_hour = 23
end_hour = 6
credit_cap_percent = 75
model_preference = "sonnet"
max_budget_per_task_usd = 0.50

[scanner]
scan_roots = [
  "~/Documents/ClaudeExperiments/PersonaBuilder",
  "~/Documents/ClaudeExperiments/Omnichannel-Platform",
  "~/Documents/ClaudeExperiments/Activslot",
  "~/Documents/ClaudeExperiments/AIFriend",
  "~/Documents/ClaudeExperiments/ScreeningAnalysis"
]
# Explicitly excluded: GitCode (office projects)
exclude_patterns = ["**/node_modules/**", "**/GitCode/**"]
skip_npm_audit = false
max_todos_per_project = 50

[subscription]
tier = "max5"

[credits]
window_reset_hour = 0
hard_stop_minutes_before = 30
# Estimated balance — will be replaced by real API data when available
estimated_balance_usd_cents = 5000
